// import { _glob } from "../src/core/services/_glob.js";

// describe("_glob should return {success: true, value: string[]}", () => {
//     test("success should be true", async () => {
//         const result = await _glob("**/*");
//         expect(result.success).toBe(true);
//         expect(result.value instanceof Array).toBe(true);
//     });
// });
